# PQC Migration Risk Framework

This repository contains the simulation and scenario analysis framework to assess migration risks in a post-quantum cryptography (PQC) environment.

## How to Run

1. Install dependencies:
   ```bash
   pip install -r requirements.txt
   ```
2. Run the simulation and generate figures:
   ```bash
   python main.py
   ```
